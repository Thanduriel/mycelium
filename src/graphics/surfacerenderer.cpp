#include "surfacerenderer.hpp"

namespace graphics {

/*	void SurfaceRenderer::draw(const Texture2D& _texture, const glm::mat4& _transform)
	{
		m_surfaces.emplace_back(_texture, _transform);
	}

	void SurfaceRenderer::present(const Camera& _camera)
	{
		m_surfaces.clear();
	}*/
}